# Texto que se queda escrito en una vista publica, y por que.
#
# Una linea por texto, con su motivo detras del `#`. Mismo criterio que
# RUTAS-ABIERTAS y SALIDAS-AL-MUNDO: no se prohibe la excepcion, se EXIGE QUE
# ESTE ESCRITA. Un nombre de marca no se traduce, y decirlo una vez vale mas que
# discutirlo cada vez.

https://…  # Es un ejemplo de direccion, no una frase: se escribe igual en cualquier idioma, y traducir el `https://` seria absurdo. Va en el `placeholder` del campo Web.
